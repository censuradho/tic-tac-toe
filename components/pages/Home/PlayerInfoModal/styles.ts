import { styled } from "stitches.config";

export const Container = styled('div', {
  width: '100vw',
  maxWidth: '40rem'
})

export const Title = styled('strong', {
  fontSize: '$lg',
  fontWeight: 500
})